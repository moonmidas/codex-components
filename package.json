{
  "name": "codexmod-components",
  "version": "0.1.0",
  "description": "Codex++ bootstrap plus the CodexMod Components tweak.",
  "type": "module",
  "bin": {
    "codexmod-components": "./scripts/install.mjs"
  },
  "scripts": {
    "install:local": "node ./scripts/install.mjs",
    "check": "node --check ./scripts/install.mjs && node --check ./tweaks/codexmod-components/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
