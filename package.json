{
  "name": "codex-components",
  "version": "0.2.0",
  "description": "Codex++ bootstrap plus the Codex Components tweak.",
  "type": "module",
  "bin": {
    "codex-components": "./scripts/install.mjs"
  },
  "scripts": {
    "install:local": "node ./scripts/install.mjs",
    "test": "node --test scripts/install.test.mjs tweaks/codex-components/index.test.cjs",
    "check": "node --check ./scripts/install.mjs && node --check ./tweaks/codex-components/index.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
