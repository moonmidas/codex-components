{
  "name": "codex-components",
  "version": "0.1.0",
  "description": "Codex++ bootstrap plus the Codex Components tweak.",
  "type": "module",
  "bin": {
    "codex-components": "./scripts/install.mjs"
  },
  "scripts": {
    "install:local": "node ./scripts/install.mjs",
    "check": "node --check ./scripts/install.mjs && node --check ./tweaks/codex-components/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
